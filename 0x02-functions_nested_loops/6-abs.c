#include "main.h"

/**
 * _abs - Entry point
 * Description: compute absolute value of integer
 * Return: 0
 * @a: checker to be checked.
 */
int _abs(int a)
{
	if (_abs(a < 0))
	{
		_putchar(a);
	}
	else if (_abs(a > 0))
	{
		_putchar(a);
	}
	else
	{
		_putchar('0');
	}
return (0);
}
